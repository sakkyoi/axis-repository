---
interface Props {
  variant: string | undefined;
  version: string | undefined;
}

const { variant, version } = Astro.props;
---
<!-- Task 4 replaces this with real conditional rendering and a test suite. -->
